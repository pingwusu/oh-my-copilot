/**
 * Error Aggregator Constants
 */

/** Maximum size of errors.jsonl before rotation (10 MB) */
export const ROTATION_BYTES = 10 * 1024 * 1024;

/** State directory name under .omcp/state/ */
export const STATE_DIR = "errors.jsonl";
