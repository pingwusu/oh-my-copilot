/**
 * Idle Alert Constants
 */

/** Default idle threshold in milliseconds (5 minutes) */
export const DEFAULT_THRESHOLD_MS = 5 * 60 * 1000;

/** State sub-directory under .omcp/state/ */
export const STATE_SUBDIR = "idle-alert";
