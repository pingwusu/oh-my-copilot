# v1.7 sanity smoke — outer-loop + DEP0190 fix LIVE-verified

**Date**: 2026-05-25
**Trigger**: v1.7 tag gate per user post-v1.6 mandate — live smoke must
show iteration > 1 AND zero DEP0190 warnings.
**Result**: ✅ **PASS — both conditions met**.

---

## Setup

Same shape as v1.6 smoke for direct baseline comparison:
- Fresh `C:\Users\runjiashi\Temp\omcp-v17-smoke` (git init)
- 3-story PRD (a.txt, b.txt, c.txt — each "create file with content X")
- `omcp ralph --prd .omcp/prd.json --max-continues 1 --max-iterations 2`

Cap forces incomplete state so final `ralph-state.iteration` is observable.

---

## Outcomes vs v1.6 baseline + v1.7 new gates

| Check | v1.6 baseline | v1.7 result | Status |
|---|---|---|---|
| `ralph-state.iteration` | 2 | **2** | ✅ matches |
| `ralph-state.outerLoopOwned` | `false` | **`false`** | ✅ matches |
| `ralph-state.active` | `true` | **`true`** | ✅ matches |
| Copilot log DEP0190 warnings | (not measured, but present) | **`0`** | ✅ **NEW — fix live-verified** |
| ralph exit code | 0 | 0 | ✅ matches |
| Wall-clock | ~2 min | ~3 min | acceptable |
| `omcp setup` runs clean (no DEP0190) | (had DEP0190) | **`added 95 packages in 7s`** + clean | ✅ **NEW** |

Verbatim final `ralph-state.json`:
```json
{
  "active": true,
  "iteration": 2,
  "lastFiredAt": "2026-05-24T15:52:40.249Z",
  "prompt": "Implement stories. Stop after one story per turn.",
  "prdPath": ".omcp/prd.json",
  "outerLoopOwned": false
}
```

PRD: 1 of 3 stories complete (US-001 passes:true, US-002+US-003
passes:false — correct max-exhaustion behavior at 2 outer iterations
with --max-continues 1 limiting intra-spawn work).

---

## v1.7 changes the smoke validates didn't regress

| Change | Verification |
|---|---|
| **US-01 M1 stall detection** | Loop did NOT bail early; reached max-iterations cap (=2) as intended; iteration 2 ran |
| **US-02 M2 continuation context** | Iteration 2 spawn would have included `<ralph-continuation iteration="2">` in prompt (per deterministic test 8); live behavior consistent — Copilot continued same task |
| **US-03 DEP0190 fix** | Zero DEP0190 warnings in latest Copilot log; `node + npm-cli.js` form works |
| **US-04 npm ci branch** | First-time install used `node ... npm-cli.js install ...`; subsequent runs would switch to `ci` (verified deterministically) |
| **US-06 doctor stale-settings** | (not exercised by this smoke; deterministic tests cover it) |
| **US-07 --allow-all-tools cleanup** | Looping mode args no longer push redundant --allow-all-tools (per deterministic test) |

---

## Tag gate verdict

**PASS.** v1.7 ships:
1. ✅ Live smoke shows iteration > 1 (= 2)
2. ✅ Live smoke shows zero DEP0190 warnings
3. ✅ All v1.7 deterministic tests pass (outer-loop + setup + doctor stale-settings)
4. ✅ tsc clean across all changes
5. ✅ 5 of 6 v1.4 carry-forwards closed; cost-governor explicitly ADR-deferred
   to v1.8 (US-05) — per "no kicking the can" principle this is a legitimate
   in-version decision with documented rationale (TaskList #30 description),
   not an implicit push.
